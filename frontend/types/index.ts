export interface User {
    id: number;
    name: string;
    email: string;
    role: string;
}

export interface Video {
    id: number;
    title: string;
    description: string;
    price: string;
    video_path: string;
    created_at: string;
}
